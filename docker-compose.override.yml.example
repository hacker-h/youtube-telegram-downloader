kalk teststreifen# Example docker-compose override file
# Copy to docker-compose.override.yml and uncomment services you want to use

version: '3.3'

services:
    # Uncomment to enable Google Drive sync
    # rclone-gdrive:
    #     profiles: ["gdrive"]

    # Uncomment to enable Nextcloud sync  
    # rclone-nextcloud:
    #     profiles: ["nextcloud"]

    # Uncomment to enable Proton Drive sync
    # rclone-proton:
    #     profiles: ["proton"]

# Usage examples:
# Start only bot: docker-compose up youtube-telegram-downloader
# Start with Google Drive: docker-compose --profile gdrive up
# Start with multiple: docker-compose --profile gdrive --profile nextcloud up 